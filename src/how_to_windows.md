To run on windows install:
Xming

On WSL
sudo apt install fontconfig

add to ~/.bashrc
export DISPLAY=:0.0

install libraries for gmsh # maybe dont need if install gmsh using apt
sudo apt-get install libglu1

install gmsh
sudo apt install gmsh